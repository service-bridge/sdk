syntax = "proto3";

package sb.vectors.optional;

message OptionalRequest {
  optional string note    = 1;
  optional int32  count   = 2;
  string          plain   = 3;

  oneof choice {
    bool   yes = 4;
    string no  = 5;
  }
}

message OptionalResponse {
  bool ok = 1;
}
