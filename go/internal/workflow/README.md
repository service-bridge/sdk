# workflow

## Зона ответственности

Замораживание объявленного графа: проверка того, что рантайм принял бы его, сборка канонических байт, которые едут в `IncomingMethod{type=WORKFLOW}.input_schema_json`, отпечаток этих байт для `contract_hash`, и вычисление выражений графа против состояния прогона.

Не делает: не объявляет типы графа (это публичный пакет `workflow/`), не регистрирует workflow (регистрация идёт одним кадром через `internal/registry`), не владеет каналом и идентичностью (`internal/connection`).

Исполняющая половина модуля — приём назначений, обход DAG, чекпойнты в рантайм — описана ниже отдельно и живёт в `runner.go`, `subscriber.go`, `ops.go`.

## Публичный контракт

Публичный для других пакетов SDK; наружу модуля ничего из этого файла не реэкспортируется.

### Замораживание графа

| Имя | Тип | По умолчанию | Что делает |
|-----|-----|--------------|------------|
| `Freeze(name string, def wf.Definition)` | `(Frozen, error)` | — | Единственный путь графа на провод: валидирует, раскладывает политику ретраев по шагам, кодирует и хеширует. Невалидный граф зарегистрировать нельзя. |
| `Frozen` | struct | — | `Name`, `Steps` (граф после раскладки ретраев — по нему работает раннер), `JSON` (канонические байты), `Fingerprint`. |
| `Fingerprint(canonicalJSON []byte)` | `string` | — | SHA-256 в hex от ровно тех байт, что уедут в `input_schema_json`. |
| `Validate(name string, def wf.Definition)` | `error` | — | Всё, что решается без запуска. Вызывается внутри `Freeze`; отдельно нужен, когда граф проверяют, не замораживая. |

### Ошибки объявления

| Имя | Тип | По умолчанию | Что делает |
|-----|-----|--------------|------------|
| `ValidationError` | struct | — | `Workflow`, `Step`, `Field`, `Reason`. Позволяет `errors.As` отличить отказ графа от прочих ошибок, а `errors.Is` — разобрать причину. |
| `ErrEmptyWorkflowName` | `error` | — | Имя workflow пустое. |
| `ErrNoSteps` | `error` | — | В графе нет ни одного шага. |
| `ErrStepID` | `error` | — | Идентификатор шага не подходит под `^[a-z0-9_]+$`. |
| `ErrDuplicateStepID` | `error` | — | Идентификатор уже занят, вложенность включительно. |
| `ErrUnknownStepKind` | `error` | — | Шаг отсутствует или не входит в набор видов. |
| `ErrDepth` | `error` | — | Групп вложено больше, чем принимает рантайм (10 уровней). |
| `ErrStepCount` | `error` | — | Шагов больше, чем принимает рантайм (500). |
| `ErrEmptyGroup` | `error` | — | В группе нет шагов. |
| `ErrUnknownDependency` | `error` | — | `waitFor` называет шаг, которого в графе нет. |
| `ErrDependencyCycle` | `error` | — | `waitFor` образует цикл. |
| `ErrCompensationUnsupported` | `error` | — | Компенсация объявлена не на `call` и не на `publish`. |
| `ErrSelfReference` | `error` | — | Шаг `workflow` запускает тот же workflow, которому принадлежит. |
| `ErrEmptyTarget` | `error` | — | Цель шага пуста. |
| `ErrNegativeDuration` | `error` | — | Отрицательная длительность сна. |
| `ErrForEachAlias` | `error` | — | Алиас развёртки не подходит под алфавит идентификатора. |
| `ErrMissingFunc` | `error` | — | У локального шага нет функции. |

### Выражения

| Имя | Тип | По умолчанию | Что делает |
|-----|-----|--------------|------------|
| `ValidatePath(expr wf.Path)` | `error` | — | Разбор без обращения к состоянию. Проверка времени объявления. |
| `EvalPath(expr wf.Path, state map[string]any)` | `(any, error)` | — | Разрешает путь. Отсутствующее значение — `nil`, не ошибка. |
| `EvalValue(v any, state map[string]any)` | `(any, error)` | — | Разрешает дерево значения: каждый `Path` внутри становится тем, на что указывает, остальное остаётся как написано. |
| `EvalTarget(t wf.Target, state map[string]any)` | `(string, error)` | — | Разрешает цель шага. `Name` — уже ответ; `Path` обязан привести к строке. |
| `EvalPredicate(p wf.Predicate, state map[string]any)` | `(bool, error)` | — | Решает, выполняется ли шаг. `nil` — условия нет, шаг выполняется. |
| `PathError` | struct | — | `Expr`, `Reason`. Называет само выражение, чтобы автор графа видел, какое именно. |
| `ErrPathSyntax` | `error` | — | Выражение не разбирается. |
| `ErrPathType` | `error` | — | `[*]` применён к не-массиву. |
| `ErrGraphValue` | `error` | — | Значение не имеет JSON-формы (канал, функция, комплексное число, не-строковый ключ карты, `NaN`/`Inf`). |
| `ErrTargetShape` | `error` | — | Цель отсутствует или разрешилась не в строку. |
| `ErrPredicateShape` | `error` | — | Предикат не относится ни к одной из шести форм. |

## Приватный контракт

| Имя | Тип | По умолчанию | Что делает |
|-----|-----|--------------|------------|
| `maxDepth`, `maxSteps` | `int` | `10`, `500` | Потолки, которые держит рантайм (`runtime/internal/workflow/register.go`). |
| `stepIDPattern` | `*regexp.Regexp` | `^[a-z0-9_]+$` | Алфавит, которым рантайм ключует шаги. |
| `validator` | struct | — | Состояние обхода: занятые идентификаторы, зависимости, порядок объявления, счётчик шагов. |
| `materializeRetry(steps, policy)` | `[]wf.Step` | — | Раскладывает политику графа по операционным шагам без своей. Группы и ожидания пропускает. Объявленный граф не мутирует. |
| `canonicalStep`, `encodeKind`, `encodeGroup` | функции | — | Сборка узла шага: общие поля, потом поля вида. |
| `canonicalValue(v any)` | `(any, error)` | — | Дерево значения на провод: `Path` становится выражением, строка, похожая на выражение, оборачивается в `{"literal": …}`. |
| `canonicalTarget(t wf.Target)` | `any` | — | Цель на провод. Неизвестный тип — `panic`: набор закрыт, а валидация прошла раньше. |
| `escapeLiteral(s string)` | `any` | — | Обёртка литерала, который иначе прочитался бы как выражение. |
| `encodeCanonical(v any)` | `([]byte, error)` | — | JSON без пробелов, с отсортированными ключами и без HTML-экранирования. |
| `walkValue(v, onPath, onText)` | `(any, error)` | — | Один обход дерева значения на два применения: канонизация и вычисление отличаются только тем, во что превращается лист. |
| `parsePath`, `pathToken` | функция, struct | — | Разбор выражения в сегменты: поле, индекс, звёздочка. |
| `fieldOf`, `asSlice`, `asStringMap`, `asNumber` | функции | — | Чтение состояния через рефлексию: состояние приходит из JSON, но локальный шаг может положить туда Go-значения. |
| `jsonEqual(a, b any)` | `bool` | — | Сравнение так, как его видит JSON: число из разобранного состояния равно тому же числу, записанному в графе. |
| `isTruthy(v any)` | `bool` | — | Истинность значения для `Truthy`. |

## Архитектурные решения и почему

**Политика ретраев раскладывается по шагам.** Рантайм заводит бюджет попыток из блока `retry` самого шага и не смотрит на граф (`register.go`, `walkSeed`). Политика, оставленная на уровне графа, — объявление, которое никто не читает. `Freeze` копирует её на `call`, `publish`, `workflow` и `local`, у которых своей нет. Группы пропускает: группа не операция, её падение — это падение ребёнка, уже отретраенное на своём бюджете. Ожидания пропускает: припаркованный шаг рантайм возобновляет, а не перевыполняет. Политика уровня графа при этом остаётся в каноническом JSON — рантайм держит её на прогон.

**Каноническая форма — `map[string]any`, а не структуры с тегами.** Кодировщик Go сортирует ключи карты сам, поэтому сортировка достаётся бесплатно и одинаково на любой глубине. Структуры дали бы порядок объявления полей — детерминированный, но не сортированный, и разошлись бы с формой, которую производят остальные SDK.

**HTML-экранирование выключено.** Иначе `<`, `>` и `&` в значении дали бы байты, отличные от тех, что производит `JSON.stringify`, и одинаковый граф получил бы разные отпечатки в разных SDK.

**`waitFor` сортируется.** Это множество, а не последовательность. Без сортировки перестановка двух зависимостей двигала бы отпечаток, то есть выглядела бы как смена контракта.

**Отсутствующий путь — `nil`, а не ошибка.** Шаг мог быть пропущен по условию: его выхода просто нет. Ошибка здесь роняла бы прогон на каждой ветке, которая не сработала. Ошибкой остаётся только то, что решается при объявлении, — синтаксис выражения, — и `[*]` над не-массивом, то есть неверно написанный граф.

**Схема входа не проходит через кодировщик выражений.** `inputSchema` — это JSON Schema, а не дерево выражений: она едет как написана, сортируются только её ключи.

**Непрозрачное значение едет как написано.** Структура, `json.RawMessage`, срез байт — данные, а не выражения. `Path`, закопанный в поле Go-структуры, неотличим от строки после кодирования, поэтому выражения распознаются только в деревьях из карт, срезов и скаляров.

**Каноническая форма и вычисление ходят по одному дереву.** `walkValue` параметризован тем, во что превращается лист. Две копии обхода разошлись бы на первом добавленном типе.

## Зависимости

Опирается на: `workflow/` (типы графа), стандартную библиотеку (`crypto/sha256`, `encoding/json`, `reflect`, `regexp`).

На него опираются: раннер и подписчик того же пакета, домен workflow (объявление хендлера) и `internal/registry` — через `Frozen.JSON` и `Frozen.Fingerprint`, которые уезжают в `input_schema_json` и `contract_hash`.
